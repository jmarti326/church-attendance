# Database URL for Prisma (PostgreSQL)
# Local: use a local PostgreSQL instance or Docker
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/church"

# Production uses Neon managed PostgreSQL (set via GitHub Secrets + Vercel env vars)
# Never commit production credentials to this file
